# Smart-Counter Docker Compose Configuration
# Copy this file to .env and customize as needed: cp .env.example .env

# ==================== DETECTOR CONFIGURATION ====================

# Path to ONNX model (relative to /app inside container)
MODEL_PATH=models/yolov8s.onnx

# Path to input video (relative to /app inside container)
INPUT_VIDEO=data/videos/853889-hd_1920_1080_25fps.mp4

# Path to output video (relative to /app inside container)
OUTPUT_VIDEO=data/output/output.mp4

# Path to SQLite database (relative to /app inside container)
DB_PATH=logs/analytics.db

# Run in headless mode (no display window)
# Set to "true" for headless, remove or set to "false" for display mode
HEADLESS_MODE=true

# Loop video infinitely (for camera-like streaming)
# Set to "true" to enable loop, "false" to process once and exit
LOOP_VIDEO=true

# Use CPU instead of GPU
# Set to "true" to force CPU, remove or set to "false" to use GPU
USE_CPU=false

# Display variable (for X11 forwarding)
DISPLAY=:0

# ==================== DASHBOARD CONFIGURATION ====================

# Dashboard port (exposed to host)
DASHBOARD_PORT=8501

# Path to database for dashboard (relative to /app inside container)
DASHBOARD_DB_PATH=/app/logs/analytics.db

# Dashboard refresh interval in seconds
REFRESH_INTERVAL=2

# Maximum number of records to display in dashboard
DATA_LIMIT=100

# ==================== EXAMPLES ====================

# Example 1: Process custom video with YOLOv8n model
# MODEL_PATH=models/yolov8n.onnx
# INPUT_VIDEO=data/videos/my_custom_video.mp4
# OUTPUT_VIDEO=data/output/custom_output.mp4

# Example 2: Run with display (non-headless) and loop
# HEADLESS_MODE=false
# LOOP_VIDEO=true
# DISPLAY=:0

# Example 3: Process video once without looping
# LOOP_VIDEO=false

# Example 4: CPU-only processing
# USE_CPU=true

# Example 5: Custom dashboard settings
# DASHBOARD_PORT=8080
# REFRESH_INTERVAL=5
# DATA_LIMIT=200

# Example 6: Different database for testing
# DB_PATH=logs/test_analytics.db
# DASHBOARD_DB_PATH=/app/logs/test_analytics.db
